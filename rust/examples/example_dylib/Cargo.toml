[package]
name = "example-dylib-plugin"
version = "0.1.0"
edition = "2021"
authors = ["Continuum Team"]
description = "Example dynamic library plugin for Continuum"
license = "MIT"

[lib]
name = "example_dylib_plugin"
crate-type = ["cdylib"]

[dependencies]
serde_json = "1.0"

[dev-dependencies]
serial_test = "3.0"
